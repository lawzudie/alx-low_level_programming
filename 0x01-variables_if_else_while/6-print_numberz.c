#include <stdio.h>

/**
 * main - prints single digits base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 48; n < 48; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
